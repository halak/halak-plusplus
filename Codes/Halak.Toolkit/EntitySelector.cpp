#include <Halak.Toolkit/EntitySelector.h>

namespace Halak
{
    namespace Toolkit
    {
        EntitySelector::EntitySelector()
        {
        }

        EntitySelector::EntitySelector(uint id)
            : Service(id)
        {
        }

        EntitySelector::~EntitySelector()
        {
        }
    }
}